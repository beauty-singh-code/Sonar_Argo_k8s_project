apiVersion: v1
kind: Service
metadata:
  name: {{ include "beauty-demo-chart.fullname" . }}
  labels:
    {{- include "beauty-demo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "beauty-demo-chart.selectorLabels" . | nindent 4 }}
