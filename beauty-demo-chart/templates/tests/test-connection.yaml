apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "beauty-demo-chart.fullname" . }}-test-connection"
  labels:
    {{- include "beauty-demo-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "beauty-demo-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
